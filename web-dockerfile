# new-web-dockerfile
FROM nginx:alpine

# Install envsubst (part of gettext package)
RUN apk add --no-cache gettext

# Remove default nginx site
RUN rm /etc/nginx/conf.d/default.conf

# Custom nginx config
COPY website/nginx.conf /etc/nginx/conf.d/youth.conf

# Copy static website files
COPY website /usr/share/nginx/html

# Copy entrypoint script
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

# Expose HTTP port
EXPOSE 80

# Use entrypoint script to inject environment variables and start nginx
ENTRYPOINT ["/entrypoint.sh"]

# docker build -f new-web-dockerfile -t youth-web .
# docker run --rm -p 80:80 youth-web

# docker image build -t docker.lthome.us:5000/youth-permission-tracker/youth-web:latest -f web-dockerfile .
# docker push docker.lthome.us:5000/youth-permission-tracker/youth-web:latest