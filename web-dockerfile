# Use official nginx image
FROM nginx:alpine

# Set working directory
WORKDIR /usr/share/nginx/html

# Remove default nginx page
RUN rm -rf ./*

# Copy website files into nginx's web root
COPY ./website/ .

# Expose port 80
EXPOSE 80

# Nginx serves index.html automatically
CMD ["nginx", "-g", "daemon off;"]
